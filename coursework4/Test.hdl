CHIP Test {
      IN plaintext[16], key[8];
      OUT ciphertext[16];

      PARTS:
      DFF(x = plaintext[0], out = in0);

      And16(x=plaintext, y=plaintext, out=left);
      And16(x=plaintext, y=plaintext, out=right);

      And(x=key[0], y=key[0], out[0]);
      And(x=key[0])


      And()




}
